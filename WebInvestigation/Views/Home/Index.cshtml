@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <h1 class="display-4">Maintenance Menu</h1>
        <p>
            @Html.ActionLink("Http Information", "", "Info")
        </p>
        <p>
            @Html.ActionLink("Ping to 127.0.0.1", "Ping", new PingModel
            {
                Ip = "127.0.0.1",
            })
        </p>
        <p>
            @Html.ActionLink("Resolve host name localhost", "Dns", new DnsModel
            {
                Host = "localhost",
            })
        </p>
        <p>
            @Html.ActionLink("Azure SQL Server Control", "", "SqlServer")
        </p>
    </div>
